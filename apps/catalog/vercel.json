{
  "buildCommand": "npx prisma generate --schema=../../libs/database/prisma/schema.prisma && npx nx build catalog --prod",
  "outputDirectory": "../../dist/apps/catalog/.next",
  "framework": "nextjs",
  "installCommand": "npm ci --legacy-peer-deps",
  "devCommand": "npx nx serve catalog",
  "regions": [
    "iad1"
  ]
}