# =============================================================================
# CMS Catalogo White Label - Environment Variables
# =============================================================================
# Copy this file to .env and update values as needed:
#   cp .env.example .env
#
# IMPORTANT: Never commit .env to version control!
# =============================================================================

# =============================================================================
# PostgreSQL Database Configuration
# =============================================================================
# Username for PostgreSQL authentication
POSTGRES_USER=cms_user

# Password for PostgreSQL authentication
# IMPORTANT: Change this in production!
POSTGRES_PASSWORD=cms_password

# Database name
POSTGRES_DB=cms_database

# Prisma Database Connection URL
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql://cms_user:cms_password@localhost:5432/cms_database

# Direct URL for migrations (optional, use if connection pooling is enabled)
# DIRECT_URL=postgresql://cms_user:cms_password@localhost:5432/cms_database

# =============================================================================
# Redis Configuration
# =============================================================================
# Redis connection URL (used by apps for caching)
REDIS_URL=redis://localhost:6379

# =============================================================================
# Application Configuration
# =============================================================================
# Node environment (development, production, test)
NODE_ENV=development

# Admin app port
ADMIN_PORT=8000

# Catalog app port
CATALOG_PORT=8001

# =============================================================================
# NextAuth Configuration (Admin App)
# =============================================================================
# NextAuth URL - Full URL of your admin app
# Development: http://localhost:8000
# Production: https://admin.yourdomain.com
NEXTAUTH_URL=http://localhost:8000

# NextAuth Secret - Generate with: openssl rand -base64 32
# IMPORTANT: Change this in production!
NEXTAUTH_SECRET=your-super-secret-nextauth-key-change-in-production

# =============================================================================
# Public URLs
# =============================================================================
# Catalog app public URL (used by admin for preview and links)
# Development: http://localhost:8001
# Production: https://catalog.yourdomain.com
NEXT_PUBLIC_CATALOG_URL=http://localhost:8001

# Admin app public URL (used by catalog for login redirection)
# Development: http://localhost:8000
# Production: https://admin.yourdomain.com
NEXT_PUBLIC_ADMIN_URL=http://localhost:8000

# =============================================================================
# Security (Future Use)
# =============================================================================
# JWT secret for authentication (generate a secure random string)
# JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Session secret
# SESSION_SECRET=your-session-secret-change-in-production
