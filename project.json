{
  "name": "cms-catalogo-white-label",
  "$schema": "./node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo 'Root project has no source to lint - using apps and libs instead'"
      }
    }
  }
}
